name: 链上数据同步
on:
  schedule:
    - cron: "*/5 * * * *" # 每5分钟执行一次
  workflow_dispatch: # 手动触发

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 调用同步接口
        run: curl https://你的vercel域名/api/sync
